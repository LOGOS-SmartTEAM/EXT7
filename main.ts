/**
 * SmartTEAM 7 — extensión MakeCode para micro:bit.
 *
 * Subconjunto de STV2-PADRE: mismos bloques, misma lógica real (I2C/GPIO),
 * pero sin las categorías VARIABLES ni ESPECIAL, y dentro de SENSORES y
 * PANTALLAS solo un subconjunto de bloques (ver README.md para el detalle).
 *
 * Estructura:
 *   - blocks/categorias/bloques.ts → categoría padre "SmartTEAM 7" + groups (subcategorías)
 *   - blocks/sabana/               → un archivo .ts por componente/bloque
 *
 * Todo el traspaso de lógica se hizo desde STV2-PADRE (fuente canónica).
 */
